extends: default

ignore: |
  target/
  site/
  wiki/

rules:
  document-start: disable
  comments-indentation: disable
  line-length:
    max: 160
    level: warning
  truthy:
    check-keys: false
    allowed-values:
      - "true"
      - "false"
      - "on"
      - "off"
